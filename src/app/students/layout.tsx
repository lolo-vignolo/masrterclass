const studentsLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <div style={{ textAlign: "center", height: "30rem" }}>
      <h1 style={{ color: "red" }}>Studentsssssss</h1>
      {children}
    </div>
  );
};

export default studentsLayout;
